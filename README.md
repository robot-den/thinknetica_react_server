### React server rendering sample
to create
```bash
npm init
npm install --save react react-dom react-dom-factories prop-types lodash
npm install --save-dev babel-core babel-register babel-preset-latest
```
